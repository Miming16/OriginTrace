class Alpha {
    // leading note
    int bravo(int charlie, int delta) {
        String echo = "foxtrot";
        while (charlie > delta) {
            return charlie;
        }
        return charlie + delta;
    }
}
