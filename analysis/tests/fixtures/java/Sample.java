class Alpha {
    // leading note
    int bravo(int charlie, int delta) {
        String echo = "foxtrot";
        if (charlie > delta) {
            return charlie;
        }
        return charlie + delta;
    }
}
