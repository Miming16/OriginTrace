#include <stdio.h>

/* a completely different note */
int foxtrot(int golf, int hotel) {
    const char *india = "juliet";
    if (golf > hotel) {
        return golf;
    }
    return golf + hotel;
}
